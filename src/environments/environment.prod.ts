/**
 * Configuración de producción.
 * Sustituye apiUrl por la URL que Render asigne al Web Service del backend.
 */
export const environment = {
  production: true,
  apiUrl: 'https://shelfy-backend.onrender.com/api',
};
